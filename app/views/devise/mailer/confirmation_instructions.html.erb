<p><%= t('.greeting', :recipient => @resource.nickname, :default => "Welcome #{@resource.nickname}!") %></p>

<p><%= t('.instruction', :default => "You can confirm your account email through the link below:") %></p>

<p><%= link_to t('.action'), confirmation_url(@resource, :confirmation_token => @token) %></p>
